<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:padding="16dp"
    tools:showIn="@layout/activity_jumping_rope">

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/reset_button"
        android:text="@string/button_reset_timer"
        android:gravity="center_horizontal"
        android:textSize="20sp"
        />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/clock_text_view"
        android:layout_below="@id/reset_button"
        android:layout_marginTop="16dp"
        android:gravity="center_horizontal"
        android:textColor="@color/colorPrimary"
        android:textSize="80sp"
        android:text="20:00:00"
        />

        <ImageButton
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/start_button"
            android:layout_below="@id/clock_text_view"
            android:src="@drawable/ic_play_arrow_black_24dp"
            android:padding="16dp"
            android:layout_alignLeft="@id/clock_text_view"/>
    <ImageButton
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/pause_button"
        android:layout_below="@id/clock_text_view"
        android:src="@drawable/ic_pause_black_24dp"
        android:layout_alignRight="@id/clock_text_view"
        android:padding="16dp"/>

</RelativeLayout>